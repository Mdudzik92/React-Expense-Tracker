export default (state, action) => {
  switch(action.type) {
    default: 
      return state;
  }
}


// A reducer is how you specify the application's state changes in response to certain actions to the context